default_model = "kimi-code/k3"
default_permission_mode = "auto"
default_plan_mode = true
merge_all_available_skills = true
telemetry = false

[providers."managed:kimi-code"]
type = "kimi"
# base_url = "https://api.kimi.com/coding/v1"
# api_key = ""

[models."kimi-code/k3"]
provider = "managed:kimi-code"
model = "k3"
max_context_size = 1048576
capabilities = [ "thinking", "always_thinking", "image_in", "video_in", "tool_use" ]
display_name = "K3"
support_efforts = [ "max" ]
default_effort = "max"

[models."kimi-code/k3".overrides]
support_efforts = [ "max" ]
default_effort = "max"

[models."kimi-code/kimi-for-coding"]
provider = "managed:kimi-code"
model = "kimi-for-coding"
max_context_size = 262144
capabilities = [ "thinking", "always_thinking", "image_in", "video_in", "tool_use" ]

[models."kimi-code/kimi-for-coding-highspeed"]
provider = "managed:kimi-code"
model = "kimi-for-coding-highspeed"
max_context_size = 262144
capabilities = [ "thinking", "always_thinking", "image_in", "video_in", "tool_use" ]

[thinking]
enabled = true
effort = "max"
keep = "all"

[loop_control]
max_retries_per_step = 10
reserved_context_size = 50000

[background]
max_running_tasks = 4
keep_alive_on_exit = false

[services.moonshot_search]
base_url = "https://api.kimi.com/coding/v1/search"
api_key = ""

[services.moonshot_fetch]
base_url = "https://api.kimi.com/coding/v1/fetch"
api_key = ""

[[permission.rules]]
decision = "allow"
pattern = "Read"

[[permission.rules]]
decision = "allow"
pattern = "AgentSwarm"

[[permission.rules]]
decision = "deny"
pattern = "Bash(rm -rf*)"

[[hooks]]
event = "PreToolUse"
matcher = "Bash"
command = 'h="$(git rev-parse --show-toplevel 2>/dev/null)/.hooks/block_git_origin_push.sh"; [ -f "$h" ] || exit 0; bash "$h"'
timeout = 10

[[hooks]]
event = "PreToolUse"
matcher = "Bash"
command = 'h="$(git rev-parse --show-toplevel 2>/dev/null)/.hooks/block_global_system_commands.sh"; [ -f "$h" ] || exit 0; bash "$h"'
timeout = 10

[[hooks]]
event = "PreToolUse"
matcher = "Bash"
command = 'h="$(git rev-parse --show-toplevel 2>/dev/null)/.hooks/block_global_package_install.sh"; [ -f "$h" ] || exit 0; bash "$h"'
timeout = 10

[[hooks]]
event = "PreToolUse"
matcher = "Bash"
command = 'h="$(git rev-parse --show-toplevel 2>/dev/null)/.hooks/block_pipe_to_shell.sh"; [ -f "$h" ] || exit 0; bash "$h"'
timeout = 10

[[hooks]]
event = "PreToolUse"
matcher = "^(Edit|Write)$"
command = 'h="$(git rev-parse --show-toplevel 2>/dev/null)/.hooks/block_dependency_folder_edits.sh"; [ -f "$h" ] || exit 0; bash "$h"'
timeout = 10

[[hooks]]
event = "PreToolUse"
matcher = "^(Edit|Write)$"
command = 'h="$(git rev-parse --show-toplevel 2>/dev/null)/.hooks/block_parent_directory_writes.sh"; [ -f "$h" ] || exit 0; bash "$h"'
timeout = 10
