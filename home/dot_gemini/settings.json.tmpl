{{- /* ~/.gemini/settings.json — Gemini CLI MCP server settings */ -}}
{
  "mcpServers": {
    "augment-context-engine": {
      "command": "auggie",
      "args": ["--mcp", "--mcp-auto-workspace"]
    }
  }
}
